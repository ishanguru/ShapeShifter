// Union 2 spheres

int scene() {
    Shape sph1;
    Shape sph2;
    Shape sphu;

    sph1 = SPHERE; 
    sph2 = SPHERE; 

    Translate(sph1, 0.0, 0.5, 0.0);
    Translate(sph2, 0.0, -0.5, 0.0);

    sphu = Union(sph1, sph2);
    print("Basic union works.");

}

