
// Call a function that returns a scaled sphere 

Shape function sphereFunc(double r) {
	Shape sph;
   	sph = SPHERE;
    Scale(sph, r, r, r);
    return sph; 
}

void scene() {
    Shape sh;
    sh = sphereFunc(r); 
}
